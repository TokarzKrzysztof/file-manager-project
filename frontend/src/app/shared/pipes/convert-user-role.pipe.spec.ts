import { ConvertUserRolePipe } from './convert-user-role.pipe';

describe('ConvertUserRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertUserRolePipe();
    expect(pipe).toBeTruthy();
  });
});
