import { ConvertSizePipe } from './convert-size.pipe';

describe('ConvertSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSizePipe();
    expect(pipe).toBeTruthy();
  });
});
