import { PaginatorIntlDirective } from './paginator-intl.directive';

describe('PaginatorIntlDirective', () => {
  it('should create an instance', () => {
    // const directive = new PaginatorIntlDirective();
    // expect(directive).toBeTruthy();
  });
});
