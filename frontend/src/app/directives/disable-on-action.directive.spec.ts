import { DisableOnActionDirective } from './disable-on-action.directive';

describe('DisableOnActionDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnActionDirective();
    expect(directive).toBeTruthy();
  });
});
